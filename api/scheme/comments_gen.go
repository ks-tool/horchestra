// Code generated by internal/gencomments; DO NOT EDIT.
// Regenerate with `make comments` after changing a doc comment on an API type.

package scheme

// goComments maps a fully qualified type or field to its Go doc comment. The reflector
// reads it to fill each schema's description, which is what a client shows when asked to
// explain a field.
var goComments = map[string]string{
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequest":                   "CertificateSigningRequest is a PEM certificate request the controller approves and signs. The approval loop auto-signs it only on the selfnodeclient rotation path: the authenticated requester is a system:nodes identity whose name equals the CSR's CN. Any other CSR is left Pending for manual (or offline-CA, node-tool) approval.",
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequestList":               "CertificateSigningRequestList is a list of CSRs.",
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequestSpec":               "CertificateSigningRequestSpec is the PEM request and the groups the requester asks for.",
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequestSpec.Groups":        "Groups the requester asks the issued cert to carry (the approval loop enforces that these are exactly system:nodes for auto-approval).",
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequestSpec.NodeName":      "NodeName is the node CN the request is for (informational; the loop derives the CN from the PEM request itself).",
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequestSpec.Request":       "Request is the PEM-encoded PKCS#10 certificate request.",
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequestStatus":             "CertificateSigningRequestStatus carries the approval decision and the issued certificate.",
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequestStatus.Certificate": "Certificate is the issued PEM client certificate, set when the CSR is Approved and signed.",
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequestStatus.Phase":       "Phase is Pending, Approved or Denied.",
	"github.com/ks-tool/horchestra/api/certificates/v1.CertificateSigningRequestStatus.Reason":      "Reason explains a Denied request, or why a Pending one is still waiting.",
	"github.com/ks-tool/horchestra/api/core/v1.Affinity":                                            "Affinity groups the placement constraints the scheduler honors: node affinity (which hosts a workload may/should run on) and workload (anti-)affinity (co-locate with / spread away from other Applications within a topology domain). Honored by the scheduler only — an author-pinned spec.nodeName skips it.",
	"github.com/ks-tool/horchestra/api/core/v1.Application":                                         "Application is the unit of work: one application is one process, run from an OCI image as a hardened supervised unit on one node. There is no containers[] list and no pod — the flat, single-container shape is the model, not a simplification of it. An author states the desired spec; the node that owns the workload reports what it observed into status, and never the other way round.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSet":                                      "ApplicationSet declares a bundle of component Applications.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSetList":                                  "ApplicationSetList is a list of ApplicationSets.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSetSpec":                                  "ApplicationSetSpec is the bundle: an explicit list of named component Applications plus shared config projected onto every child.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSetSpec.Placement":                        "Placement and Rollout are the set's own traits, in the same two sections a component carries: where the children go, and how they get there. They differ from a component's only in what they can say — a set's placement is a RELATION between children, which no single one of them could state about itself.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSetStatus":                                "ApplicationSetStatus is the observed rollup, written through the status subresource.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSetStatus.Phase":                          "Phase is the one-word rollup an operator reads first. It is not a second vocabulary: when something holds the set back it IS that condition's reason (RenderError, AwaitingAnchor, RolloutHeld), so the summary and the detail can never disagree; otherwise Ready when every rendered child exists and runs, else Progressing.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec":                                     "ApplicationSpec uses Kubernetes Pod vocabulary on a flat, single-container shape (one application = one process = one systemd unit, so there is no containers[] list). It stays inside horchestra's invariants: NodeName is author-supplied and required (no scheduler fills it); Env is an ordered list whose only indirection is a Secret reference in the app's own namespace (see EnvSecretRef); Ports are a pure declaration (no in-node data-plane); storage is a separate PersistentVolume Kind, referenced by name.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.Command":                             "Command overrides the image ENTRYPOINT and Args overrides its CMD (Kubernetes semantics). Both are literal argv — never interpolated with values.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.Env":                                 "Env are environment variables layered over the image's own, in declared order. An entry carries either a literal value (plain config — a literal is stored in the object and rendered into the unit) or a secretRef the node resolves, including \"key: *\" to import every key of a Secret. A credential is still better mounted as a file: read EnvSecretRef's doc comment for what env costs you.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.HostNetwork":                         "HostNetwork runs the workload in the HOST's network namespace: it binds the node's own addresses, its declared ports are real host ports, and it reaches whatever the node reaches — including the node's loopback and every other workload's ports.\n\nToday it is the only mode there is, so an unset field means it and an explicit `false` is REFUSED rather than accepted and quietly ignored: a field that reports isolation nobody implements is worse than no field, because it is read as a guarantee. When the pod network lands, unset flips to meaning isolation and this becomes the privileged exception it is meant to be — an edge, a node exporter — rather than the rule.\n\nA pointer so that \"said nothing\" and \"asked for isolation\" are different states. They have to be: the first is every manifest written so far and the second is a request the tree cannot honour, and answering them the same way would either refuse everything or promise something false.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.Image":                               "Image is a plain OCI image reference, e.g. reg.io/ns/app:v1 (no scheme).",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.Lifecycle":                           "Lifecycle is how this workload is run and stopped; Placement is where it may run. Both are traits: policy stated ABOUT a workload that says nothing about what the workload is, which is what separates them from the fields above and lets one be authored, reviewed and (for placement) ignored by the node entirely.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.Ports":                               "Ports the application listens on — a pure declaration for an external edge/LB (the orchestrator runs no data-plane of its own). Absent = not routed.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.RuntimeClassName":                    "RuntimeClassName selects which node runtime executes this workload (a RuntimeClass analog). Empty selects the node's default runtime. The scheduler places the app only on nodes whose status.runtimes advertises the requested class (a Filter predicate), so a class no node supports leaves the app pending rather than failing at start. Optional.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.SecurityContext":                     "SecurityContext sets the workload's identity and confinement. When unset, the image's own USER is used; the hardened floor (NoNewPrivileges, ProtectSystem, read-only rootfs) always applies regardless of this field.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.ServiceName":                         "ServiceName joins this workload to a Service, and joining is DECLARED here rather than selected there: an instance says which service it belongs to, the way a Consul instance registers under a service name, and the Service asserts nothing about a fleet it cannot see. The spelling is StatefulSet's; the meaning is Consul's — in Kubernetes the field only names a headless Service for a DNS subdomain and the selector is what confers membership, so nothing here should be read across from it.\n\nIt is a reference to a Service in this namespace, validated like any other reference, so a typo cannot conjure a service nobody can find. Empty means the workload is in no catalog and has no virtual address — which is the right answer for the edge itself and for anything else that is reached rather than reaching.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.Values":                              "Values is free-form horchestra-native config (no Pod analog).",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationSpec.Volumes":                             "Volumes are the storage this workload mounts — each entry is an inline volume (a PersistentVolume or an ephemeral tmpfs) mounted at a path. A pv volume's PersistentVolume is created implicitly on demand when none exists.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationStatus":                                   "ApplicationStatus is the node-observed status of an Application, reported by the node-agent each reconcile and persisted through the status subresource. It is authoritative only for an app pinned to a node (the reporting node); an unplaced app has an empty phase.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationStatus.Address":                           "Address is the routed address the node WIRED this workload at, in CIDR form (10.244.1.7/32). Empty on the host network, which is every workload until an operator turns routing on.\n\nThe node reports it, the control plane does not write it — the same arrangement Kubernetes has for podIP, and for the same reason: it is an observation of what exists on a machine. The control plane CHOOSES the address and sends it in the desired-state push (beside the workload tokens, which are per-workload decisions delivered the same way); what comes back here is the node saying it is so, which is also what tells a restarted control plane which addresses are in use.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationStatus.Attempts":                          "Attempts is how many times this job's node has run it, counted against spec.lifecycle.backoffLimit. It is on the object because that is the only durable place: the unit is transient, so a node that rebooted mid-budget would otherwise start counting from zero. Only a job counts — a service's restarts are systemd's and are not budgeted.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationStatus.ExitCode":                          "ExitCode is what the workload's main process returned, once it has finished. A pointer because zero is the interesting value: an absent field is a workload that has not finished, and a plain int would report every running workload as having succeeded.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationStatus.FinishedAt":                        "FinishedAt is when the workload's main process left, by the node's clock.",
	"github.com/ks-tool/horchestra/api/core/v1.ApplicationStatus.ObservedGeneration":                "ObservedGeneration is the metadata.generation the node reports having converged to. It is what names the RUNNING version: phase says a workload is up, this says which spec it is up on, so a node that cannot apply a new spec (an unpullable image, a rejected mount) keeps reporting the old generation instead of looking converged.",
	"github.com/ks-tool/horchestra/api/core/v1.ChildMeta":                                           "ChildMeta is an optional per-child metadata overlay (merged over common; child wins).",
	"github.com/ks-tool/horchestra/api/core/v1.ChildStatus":                                         "ChildStatus is one child's observed placement and phase.",
	"github.com/ks-tool/horchestra/api/core/v1.CommonMeta":                                          "CommonMeta is projected into every child: labels/annotations merged (child wins), env appended UNDER the child's own env (a child var with the same Name wins — config only, no secrets), and volumes appended deduplicated by mountPath (the child's own mount at a path wins).",
	"github.com/ks-tool/horchestra/api/core/v1.Condition":                                           "Condition is a status condition (e.g. a render or name-conflict error).",
	"github.com/ks-tool/horchestra/api/core/v1.EngineRole":                                          "EngineRole names a role on a Vault/OpenBao secrets engine: the engine's mount and the role on it. One type for both credential shapes, because only the endpoint differs.\n\nA STATIC role is the middle ground between a KV path and a full dynamic credential. Vault owns ONE fixed database user and rotates its password on its own schedule; there is no lease to hold and nothing to renew, so nothing on a node has to stay alive to keep the credential alive — but the long-lived shared password an operator forgets to change is gone all the same. What it does not give is a per-consumer identity: every workload reading the role gets the same user, so the database's audit log names the role, not the caller.\n\nA DYNAMIC role is the other half of that trade: Vault CREATES a database user per request and binds it to a lease, so each consumer is its own identity and revoking one touches nobody else. The cost is that the lease has to be held — renewed before it expires and released when the workload it belongs to goes away — which is machinery a static role needs none of.",
	"github.com/ks-tool/horchestra/api/core/v1.EnvSecretRef":                                        "EnvSecretRef sources an environment variable — or, with Key \"*\", one variable per key — from a Secret in the Application's OWN namespace. There is no namespace field, so a cross-namespace reference is not expressible.\n\nDelivery is REAL PROCESS ENVIRONMENT, Kubernetes-style: the workload sees the variable at execve, nothing must be sourced. The resolved values never touch persistent storage — they would otherwise outlive the agent that fetched them: survive a reboot with nothing to re-authorize them, and be copied into every backup of the node. So the agent writes them to a RAM-backed (tmpfs) carrier file, which the systemd runtime hands to PID1 via EnvironmentFile= (read at spawn; only the PATH is a unit property) and the rootless runtime's sandbox folds into the execve envp: only the file's path is ever written down. Nothing about the value reaches the unit's text or its Environment=, which systemd exports as a bus property any local caller can read with `systemctl show`. The usual env-variable caveats apply — the values are visible in the workload's own /proc/self/environ and inherited by its children; mount a secret volume instead where that is unacceptable.\n\nBecause the values exist only while the agent holds them, a workload that sources any is bound to the agent's own unit (Requires=/After=) and is never enabled into a boot target: it starts when the agent has the values, and stops when the agent does.\n\nAN ENV-SOURCED SECRET DOES NOT ROTATE. Environment is spawn-time state: nothing can replace it in a running process, so the only way to deliver a new value this way is to restart the workload — and restarting a workload because a credential rotated is a worse answer than not rotating it. The node writes the new value regardless, so the next start for any other reason uses it, and a CHANGED SET of variables does restart, because that is a different workload. A credential that must rotate under a running process is mounted as a file: a `type: secret` volume is the agent's own RAM-backed directory, bound into the workload, so a rewrite there is live immediately. This is the same split Kubernetes makes, for the same reason.",
	"github.com/ks-tool/horchestra/api/core/v1.EnvSecretRef.Key":                                    "Key is the data key to read, or \"*\" (EnvSecretAllKeys) to import every key: each becomes an assignment named after the key, in sorted key order, so the projection is deterministic. A key that is not a valid environment-variable name — a Secret key may be any file basename, e.g. \"ca.pem\" — fails the converge with that key named, rather than being skipped silently; so does a value carrying a newline, which the file's one-assignment-per-line shape cannot express. Mount such keys instead.",
	"github.com/ks-tool/horchestra/api/core/v1.EnvSecretRef.Name":                                   "Name is the Secret in the Application's namespace.",
	"github.com/ks-tool/horchestra/api/core/v1.EnvSecretRef.Optional":                               "Optional lets the application start when the Secret (or the named key) is absent. Absent or false is fail-closed: the app is held rather than started without the value, and an already-running one keeps running.",
	"github.com/ks-tool/horchestra/api/core/v1.EnvSecretRef.Prefix":                                 "Prefix is prepended to every variable name of a wildcard import (e.g. \"PG_\"). It must itself be a valid environment-variable name prefix. Ignored for a single key.",
	"github.com/ks-tool/horchestra/api/core/v1.EnvVar":                                              "EnvVar is one environment variable layered over the image's own, in declared order (Kubernetes/OCI shape). The ordered list (not a map) keeps the projected environment deterministic, which the node runtime's converge hash relies on. Exactly one source is allowed: a literal Value, or a SecretRef the node resolves.",
	"github.com/ks-tool/horchestra/api/core/v1.EnvVar.Name":                                         "Name is the variable name. Required for a literal or a single-key SecretRef; it must be EMPTY for a wildcard SecretRef, whose names come from the Secret's own keys.",
	"github.com/ks-tool/horchestra/api/core/v1.EnvVar.SecretRef":                                    "SecretRef sources the value from a Secret instead of carrying it here. Only the reference is stored; the node resolves it at converge time.",
	"github.com/ks-tool/horchestra/api/core/v1.EnvVar.Value":                                        "Value is the literal value. It is stored in the object and rendered into the unit, so it is plain config only — never a credential.",
	"github.com/ks-tool/horchestra/api/core/v1.IDRange":                                             "IDRange is a half-open block of host ids, [Min, Min+Size). It carries both blocks the system reserves: a namespace's workload ids, and the /etc/sub{u,g}id range the rootless runtime maps.",
	"github.com/ks-tool/horchestra/api/core/v1.KeyToPath":                                           "KeyToPath projects one Secret key to a relative path within a secret mount, optionally with its own file mode (else the volume's DefaultMode applies).",
	"github.com/ks-tool/horchestra/api/core/v1.Lifecycle":                                           "Lifecycle is the run-and-stop policy of a workload: whether it is a service the node keeps up or a job that is allowed to end, and how long a stop waits before it is not a stop any more. It says nothing about what the workload is, which is why it is a section and not more fields on the spec — and why a set of them can be authored once and stated about many workloads.",
	"github.com/ks-tool/horchestra/api/core/v1.Lifecycle.ActiveDeadlineSeconds":                     "ActiveDeadlineSeconds bounds a JOB's whole run — from the moment the node starts it to the moment its process leaves — after which the workload is killed and reported Failed with DeadlineExceeded. Unset means no deadline: a job may run as long as it likes.\n\nIt has no meaning for a service and admission refuses it there. A service that must not run past some age is not a service, and the field that would express it (systemd's RuntimeMaxSec) does nothing on the unit shape a job uses, so honouring it for one shape and silently ignoring it for the other is the one thing this must not do.",
	"github.com/ks-tool/horchestra/api/core/v1.Lifecycle.BackoffLimit":                              "BackoffLimit is how many times a failed JOB is retried before it is Failed for good (reason BackoffLimitExceeded). Unset or 0 is no retry, which is what restartPolicy: Never says on its own; 2 means up to three runs in total.\n\nThe count is status.attempts and it lives on the OBJECT, not on the node: a job's unit is transient and a reboot forgets it, so a node-local counter would hand every job a fresh budget every time its host came back. Like activeDeadlineSeconds it is refused on a service, whose retries are systemd's Restart= and are not counted at all.",
	"github.com/ks-tool/horchestra/api/core/v1.Lifecycle.RestartPolicy":                             "RestartPolicy is Always (default), OnFailure or Never; Never marks a run-to-completion job, which the node keeps in its terminal state instead of starting again. An absent one is filled with the default DECLARED HERE before the object is stored, so what a reader sees is what the node will do — including when the author omits the whole section, which is why the section itself is conjured before the declared defaults run.",
	"github.com/ks-tool/horchestra/api/core/v1.Lifecycle.TerminationGracePeriodSeconds":             "TerminationGracePeriodSeconds is how long a stop waits for the workload to exit on its own before it is killed. Unset means DefaultTerminationGracePeriodSeconds; 0 kills immediately.\n\nIt is a pointer so \"unset\" stays distinguishable from an explicit 0, and it is worth setting deliberately: the workload is PID 1 of its own namespace, where the kernel drops any signal it has installed no handler for, so an image that ignores SIGTERM spends this whole period running before it is killed — on every stop, and therefore on every restart a changed spec causes. Raise it for a workload that needs to flush; lower it for one that cannot shut down gracefully anyway.",
	"github.com/ks-tool/horchestra/api/core/v1.NamedApplicationSpec":                                "NamedApplicationSpec is one component of the bundle: a name, an optional per-child metadata overlay, a typed ApplicationSpec, and the scale trait — at most one fan-out mode.",
	"github.com/ks-tool/horchestra/api/core/v1.NamedApplicationSpec.Scale":                          "Scale is how many of this component the set renders. It sits on the component and not inside its spec because it is the one trait an Application cannot carry: a spec describes one workload, and there is no count of itself to put in it.",
	"github.com/ks-tool/horchestra/api/core/v1.Namespace":                                           "Namespace is the soft-multi-tenancy scope: an Application, PersistentVolume, Role or RoleBinding lives inside exactly one Namespace, addressed as .../namespaces/<ns>/<resource>. The Namespace object is itself cluster-scoped, and the set of them is flat — there is no grouping above it.",
	"github.com/ks-tool/horchestra/api/core/v1.Node":                                                "Node is a member of the fleet: a Linux host running the agent. It is created when the agent first registers with a certificate the controller trusts, and its identity is that certificate's CN — nothing an agent sends can name a different node. The agent owns status (capacity, readiness, its heartbeat) and may write nothing else; spec is the operator's, which is what makes a cordon something a node cannot lift for itself.",
	"github.com/ks-tool/horchestra/api/core/v1.NodeAffinity":                                        "NodeAffinity constrains placement by Node labels. Required must match for a node to be feasible (a Filter predicate); Preferred only ranks feasible nodes (a Score).",
	"github.com/ks-tool/horchestra/api/core/v1.NodeSelector":                                        "NodeSelector matches a Node's scheduling labels — spec.labels the operator authored plus the derived status.labels: every MatchLabels entry must equal and every MatchExpressions requirement must hold (AND across both; empty matches any).",
	"github.com/ks-tool/horchestra/api/core/v1.NodeSelectorRequirement":                             "NodeSelectorRequirement is one label constraint. Operator is In, NotIn, Exists or DoesNotExist; Values is used by In/NotIn and must be empty for Exists/DoesNotExist.",
	"github.com/ks-tool/horchestra/api/core/v1.NodeSpread":                                          "NodeSpread turns a component into a DaemonSet: one child per Node whose spec.labels match NodeSelector (empty = every Node), with spec.nodeName injected. The ONLY per-node variation is the node identity — no templating. A nodeSpread component must leave spec.nodeName empty (the set assigns it and owns placement).",
	"github.com/ks-tool/horchestra/api/core/v1.NodeStatus":                                          "NodeStatus is reported by the node-agent every reconcile. Capacity is the resource the node offers (its total, capped by any -config limit); Allocated is the sum of the requests of the applications running on it, subtracted from capacity as apps are placed. `kubectl get nodes` shows the Allocated/Capacity ratio as a percentage, and -o wide the raw amounts.",
	"github.com/ks-tool/horchestra/api/core/v1.NodeStatus.IP":                                       "IP is the node's address as it reaches the controller (its source IP toward the control plane), shown in `kubectl get nodes -o wide`.",
	"github.com/ks-tool/horchestra/api/core/v1.NodeStatus.Labels":                                   "Labels are the placement labels the CONTROL PLANE derives from the rest of this status (see DerivedNodeLabels), recomputed on every heartbeat and never taken from the node's own report. They sit beside spec.labels rather than in it because they are observation, not intent — and because the write path that carries them is the node's status subresource, which nodeRestriction confines a node to precisely so it cannot label itself into a placement pool it was never granted.",
	"github.com/ks-tool/horchestra/api/core/v1.NodeStatus.OS":                                       "OS is a human-readable identifier — distro, kernel and CPU architecture, e.g. \"Ubuntu 24.04.2 LTS (6.8.0-85-generic, amd64)\".",
	"github.com/ks-tool/horchestra/api/core/v1.NodeStatus.Platform":                                 "Platform is the node's OS and CPU architecture, reported by its agent.",
	"github.com/ks-tool/horchestra/api/core/v1.NodeStatus.Ready":                                    "Ready is the agent's self-reported readiness, refreshed every reconcile together with Heartbeat. A node counts as ready only while its heartbeat is fresh, so a stopped agent becomes NotReady on its own — without a separate liveness controller. Not omitempty: a genuine false must overwrite a stale true through the status merge patch.",
	"github.com/ks-tool/horchestra/api/core/v1.NodeStatus.RoutedNetwork":                            "RoutedNetwork is whether this node can give a workload a network of its own — it has the privileged helper, and that helper holds the capabilities the job needs. Reported by the node because it is a fact about the machine, and the only place it can be known.\n\nThe scheduler filters on it. Without that, an isolated workload lands wherever it fits and then refuses to start on a node that cannot wire it — a failure at the far end of the system from the decision that caused it.",
	"github.com/ks-tool/horchestra/api/core/v1.NodeStatus.Runtimes":                                 "Runtimes are the workload runtimes this node's agent supports (e.g. [\"systemd\"]). The scheduler filters an Application's runtimeClassName against this set; an empty set means the node advertises only its unnamed default runtime.",
	"github.com/ks-tool/horchestra/api/core/v1.PersistentVolume":                                    "PersistentVolume is a directory of storage on a node's disk, with a lifecycle independent of the Applications that mount it: deleting an Application leaves the volume and its data; deleting the PersistentVolume reclaims the data from disk on the next reconcile.",
	"github.com/ks-tool/horchestra/api/core/v1.PersistentVolumeSpec.Mode":                           "Mode is the directory's octal permission, e.g. \"0755\" (default) or \"1777\" for a shared writable directory such as a socket dir.",
	"github.com/ks-tool/horchestra/api/core/v1.PersistentVolumeSpec.Node":                           "Node is the node whose disk backs the volume; only that node provisions it.",
	"github.com/ks-tool/horchestra/api/core/v1.PersistentVolumeSpec.ReclaimPolicy":                  "ReclaimPolicy decides the fate of the data when the PersistentVolume is deleted: \"Delete\" (ReclaimDelete, the default) reclaims the backing store; \"Retain\" (ReclaimRetain) keeps the data on disk for manual recovery and the agent stops managing it. The agent records this at provision time, since a deleted PV's spec is gone by the time its data would be reclaimed.",
	"github.com/ks-tool/horchestra/api/core/v1.PersistentVolumeSpec.Shared":                         "Shared lets more than one Application mount this volume at once. Without it a volume belongs to exactly one Application, and a second one naming it is refused.\n\nExclusive is the default because concurrent writers are a property of the DATA, not of the mount: two apps on one directory is safe for a content-addressed store and silent corruption for a database, and nothing in a volume's declaration tells the two apart. Whoever authored the volume knows which it holds, so the decision is theirs.\n\nIt lives here and nowhere else on purpose. An Application's inline pv volume has no equivalent field: a workload cannot opt itself into sharing another workload's storage, and a volume created implicitly from an inline mount is exclusive with no way to ask otherwise. Turning it off again is refused while more than one Application still mounts the volume — the invariant would be false the moment the write landed.",
	"github.com/ks-tool/horchestra/api/core/v1.PersistentVolumeSpec.Size":                           "Size is the requested storage as a Kubernetes quantity (\"10Gi\"). It is advisory in this skeleton (a host directory, not a quota-enforced volume).",
	"github.com/ks-tool/horchestra/api/core/v1.Placement":                                           "Placement is where a workload may run: constraints the SCHEDULER honors, and nothing the node ever reads. That is the whole reason it is one section — every field here is spent by the time the workload reaches a node, so an operator can retune placement without touching anything the runtime acts on.\n\nNodeName is the exception to that and belongs here for exactly that reason: it is the one placement statement the SCHEDULER may also write, and it overrides every other field in the section, so an author reading a pin and the constraints it silences reads them together.",
	"github.com/ks-tool/horchestra/api/core/v1.Placement.Affinity":                                  "Affinity constrains placement by node labels and by proximity to other Applications (co-locate / spread across a topology). It is a scheduler-time constraint, not re-evaluated after placement. Optional.",
	"github.com/ks-tool/horchestra/api/core/v1.Placement.NodeName":                                  "NodeName pins the application to a single node: one application runs on exactly one node. Optional, and the one field of this section a node ever sees — it is what the desired-state push is keyed on. When empty the scheduler assigns a node by fitting the app against the rest of this section and writes it here; when the author sets it the app is pinned there and the scheduler leaves it, and the rest of the section, alone.",
	"github.com/ks-tool/horchestra/api/core/v1.Placement.NodeSelector":                              "NodeSelector is placement sugar: the app is placed only on nodes whose scheduling labels match every entry (a subset match) — equivalent to a nodeAffinity.required with these matchLabels. Optional.",
	"github.com/ks-tool/horchestra/api/core/v1.Placement.Priority":                                  "Priority is an optional scheduling priority (higher = placed and kept first). The built-in scheduler does not read it: it is carried on the object so a queue/preemption engine can be plugged in without a schema change.",
	"github.com/ks-tool/horchestra/api/core/v1.Platform":                                            "Platform is a node's operating system and CPU architecture as the Go runtime and an OCI image manifest both name them (\"linux\", \"amd64\"). It is the machine-readable half of NodeStatus.OS, which is prose for a human column and nothing should parse.",
	"github.com/ks-tool/horchestra/api/core/v1.Port":                                                "Port is one network port the application listens on, for an external edge to route to. Name is an optional label (\"http\", \"metrics\", …), free to omit for a single-port app; Port is the TCP port and is required.",
	"github.com/ks-tool/horchestra/api/core/v1.PreferredNodeTerm":                                   "PreferredNodeTerm is a soft node preference; Weight (conventionally 1..100) ranks a feasible node when its Preference matches.",
	"github.com/ks-tool/horchestra/api/core/v1.ResourceAmounts":                                     "ResourceAmounts is a set of compute resources — CPU and memory — as Kubernetes resource quantities, decoded and printed the standard way (\"500m\", \"2\", \"512Mi\", \"8Gi\"). It is both a node's capacity/allocation and an application's requests/limits. Disk is deliberately absent: per-application storage is requested through Storage []VolumeClaim, not as a compute resource.",
	"github.com/ks-tool/horchestra/api/core/v1.ResourceRequirements":                                "ResourceRequirements are an application's resource requests and limits. Requests are subtracted from a node's available capacity when the app is placed (the allocation); limits cap what it may use. A request left unset defaults to the corresponding limit.",
	"github.com/ks-tool/horchestra/api/core/v1.Rollout":                                             "Rollout is how the set converges its children to a changed spec, as opposed to what it converges them to: how much may be disrupted at once, and whether a child the set no longer renders is deleted.\n\nWhat happens to the children when the SET is deleted is deliberately NOT here: that is a fact about one removal rather than about the set, so it rides on the delete request (`kubectl delete --cascade=orphan`) where the person deciding it is the person deleting.",
	"github.com/ks-tool/horchestra/api/core/v1.Rollout.MaxUnavailable":                              "MaxUnavailable caps how many children may be un-Running simultaneously while the set rolls a change. Updates are applied to at most that many children per pass and resume only as the updated ones report Running again, so a broken version stalls the rollout instead of reaching every child. It bounds UPDATES only: creating a child that does not exist yet and pruning one the set no longer renders are not disruptions of a running workload. Unset (or 0) converges every changed child at once.",
	"github.com/ks-tool/horchestra/api/core/v1.Rollout.Prune":                                       "Prune deletes children the set no longer lists. Default true. It belongs here rather than in a policy block of its own because it is the same question the rest of this section answers — what the set is allowed to do to a running child to reach the spec.",
	"github.com/ks-tool/horchestra/api/core/v1.Scale":                                               "Scale fans a component out — into N identical children (Replicas) or one child per matching Node (NodeSpread). At most one mode; neither is a fan-out of one.",
	"github.com/ks-tool/horchestra/api/core/v1.Scale.Replicas":                                      "Replicas fans the component out into N children named <set>-<component>-<i>, i in [0,N). The ONLY per-child variation is the index — there is no templating, so every replica carries a byte-identical spec and the scheduler places each independently.\n\nSetting it (even to 1) switches the component to indexed names, so toggling it on or off RENAMES the children: the old names are pruned and the new ones created. That matters for storage — a pv volume with no name is provisioned per child from the child's name, so each replica gets its own volume (and a rename abandons the old one, keeping its data). A pv volume WITH a name is one shared volume, which is why admission refuses that combination for replicas > 1.",
	"github.com/ks-tool/horchestra/api/core/v1.Secret":                                              "Secret holds sensitive key/value data (credentials, keys, config) projected into an Application through a type:secret volume. It is namespaced and never crosses namespaces. Data values are raw bytes — base64-encoded on the JSON wire, matching Kubernetes — so the agent materializes them verbatim into a read-only in-RAM mount.",
	"github.com/ks-tool/horchestra/api/core/v1.Secret.Data":                                         "Data is the secret payload: raw bytes per key (base64 on the JSON wire). Keys must be valid path basenames.",
	"github.com/ks-tool/horchestra/api/core/v1.Secret.Immutable":                                    "Immutable, when true, forbids any change to the data after creation (metadata may still change), letting the control plane and agent treat the content as stable.",
	"github.com/ks-tool/horchestra/api/core/v1.Secret.StringData":                                   "StringData is a write-only convenience for supplying values as UTF-8 strings; the secretPolicy admission plugin merges it into Data and clears it, so a stored Secret only ever carries Data.",
	"github.com/ks-tool/horchestra/api/core/v1.Secret.Type":                                         "Type classifies the secret (SecretTypeOpaque by default, or SecretTypeVault for a reference whose value is fetched from a SecretStore rather than carried inline).",
	"github.com/ks-tool/horchestra/api/core/v1.SecretList":                                          "SecretList is a list of Secrets.",
	"github.com/ks-tool/horchestra/api/core/v1.SecurityContext":                                     "SecurityContext is the workload's identity on its node. Both fields are assigned by the control plane out of the namespace's id block — they are its output, not the author's input — and both are pinned for the object's life, because everything the workload writes is owned by them.\n\nThe confinement floor is not represented here at all, and deliberately so: no-root and no-escalation are not settings. They are enforced by ValidRunAsID at admission and again on the node, and by NoNewPrivileges plus an empty CapabilityBoundingSet that every unit carries unconditionally. A field that could only ever hold one value would advertise a choice that does not exist.",
	"github.com/ks-tool/horchestra/api/core/v1.SecurityContext.RunAsGroup":                          "RunAsGroup is the group the whole namespace shares, and the workload's primary group. With distinct uids a uid can no longer be what lets two workloads use the same PersistentVolume — this is. Volumes are chgrp'd to it and carry setgid, so what one workload writes stays in the group and stays reachable by the next one that mounts the same volume. It is the primary group rather than a supplementary one because systemd accepts an allocated primary gid for a workload with no account on the node, while a supplementary group must already exist there.",
	"github.com/ks-tool/horchestra/api/core/v1.SecurityContext.RunAsUser":                           "RunAsUser is the workload's own uid, distinct from every other workload's. That is what makes it a boundary: workloads share the host PID namespace, so a shared uid would let one reach another's rootfs, volume data and materialized secrets through /proc/<pid>/root.",
	"github.com/ks-tool/horchestra/api/core/v1.Service":                                             "Service is a name in the catalog and the address behind it — a directory, in the sense that it exists before anything is in it and independently of what is. It owns exactly two things: the name clients resolve, and the address they connect to. It owns no workloads.\n\nThere is no selector, and that absence is the design. An instance joins by DECLARING it — `Application.spec.serviceName` — the way a Consul instance registers under a service name, rather than by matching labels a service asserts about a fleet it cannot see. A selector can be squatted by a foreign object and can drift from what is actually running; a declaration cannot disagree with the declarer. It also means this object never has to be reconciled against reality: it is not a claim about the world, it is a name and an address.\n\nBecause its identity is independent of its members, an empty Service is an ordinary Service — which is what makes the address's lifetime the OBJECT's lifetime. The alternative (a service computed from whichever workloads currently exist) has to answer what happens in the seconds between the last old instance and the first new one during a rolling update, and every answer to that is a heuristic.\n\nRenaming is therefore a move, not a rename: members reference the name, so a new name is a new directory with a new address, and the instances are re-pointed by whoever wrote them.",
	"github.com/ks-tool/horchestra/api/core/v1.ServiceList":                                         "ServiceList is a list of Services.",
	"github.com/ks-tool/horchestra/api/core/v1.ServicePort":                                         "ServicePort is one port of a service.",
	"github.com/ks-tool/horchestra/api/core/v1.ServicePort.Name":                                    "Name distinguishes the ports of one service and becomes part of the catalog's service name (`<service>-<port>`), so it is DNS-1123 shaped. Optional for a single-port service.",
	"github.com/ks-tool/horchestra/api/core/v1.ServicePort.Port":                                    "Port is the port callers address. An instance may listen on a different one — see TargetPort — so that a workload's own port is not part of the service's contract.",
	"github.com/ks-tool/horchestra/api/core/v1.ServicePort.Protocol":                                "Protocol is TCP (default) or UDP.",
	"github.com/ks-tool/horchestra/api/core/v1.ServicePort.TargetName":                              "TargetName names the port ON THE INSTANCE — an entry of the Application's own `spec.ports[]`. It is a different namespace from Name above: that one is the service's name for the port and follows it into the catalog, this one is the workload's name for its own, and there is no reason a service called `http` must front a port its instances also call `http`.\n\nNaming is the useful direction because it keeps somebody else's arithmetic out of the service's contract: a workload can move its port on a rebuild and neither the Service nor anything calling it is edited. A number pins the service to today's value of an implementation detail.",
	"github.com/ks-tool/horchestra/api/core/v1.ServicePort.TargetPort":                              "TargetPort is the same thing said as a number, for an instance whose port has no name. Setting both is refused: one of them would be dead text, and which one is not obvious from reading the manifest.",
	"github.com/ks-tool/horchestra/api/core/v1.ServiceSpec":                                         "ServiceSpec is what the author declares: the ports, and the address callers reach them at. Everything an external consumer reads BESIDE those is either DERIVED or the object's own annotations: the catalog's ServiceTags are generated from what the control plane knows for certain (namespace, service, port, protocol, the bundle an instance came from), and annotations become its ServiceMeta.\n\nNobody writes a tag. An author-supplied one is a free string nobody validates, one more place for the same fact to be stale, and — the moment it carries an edge product's configuration format — the point at which the API takes sides about which edge you run. Configure the edge in the edge; this publishes facts.",
	"github.com/ks-tool/horchestra/api/core/v1.ServiceSpec.ClusterIP":                               "ClusterIP is the address callers connect to, and it is DECLARED rather than allocated: whatever balances this service already has an address — a node-local balancer bound to a loopback address, a shared HAProxy on its own IP, a keepalived pair — and this is where that address is written down so clients and the catalog agree on it.\n\nIt is optional, and empty is an ordinary Service: the catalog's names are the discovery surface either way, and a client that resolves instances by name (a balancer reading /servicediscovery, an application doing its own selection) never needs one. What an address adds is a single point to connect to when the caller cannot choose between instances itself.\n\nThe control plane does not allocate it and does not verify that anything answers there — it publishes it. An address for which nothing was deployed refuses connections, and that is a deployment's own doing, not a promise the API made. Automatic allocation belongs with the eBPF datapath, which is what makes an address real without anything binding it; until that exists, inventing one would mean handing out a value nothing in the fleet has heard of.\n\nTwo Services may share an address as long as their ports differ — one balancer commonly fronts several services — which is why the address alone is not required to be unique but (address, port) is.",
	"github.com/ks-tool/horchestra/api/core/v1.ServiceSpec.Ports":                                   "Ports are the ports this service answers on. They are the service's own vocabulary, not a copy of any workload's: an instance declares the port it listens on, and this declares the port callers ask for. A name is required once there is more than one, because the catalog splits service names per port (Consul's own answer to a multi-port service) and an unnamed second port would have nothing to be called.",
	"github.com/ks-tool/horchestra/api/core/v1.SetPlacement":                                        "SetPlacement constrains where the set's children run relative to each other. It is a separate type from a component's Placement rather than a superset of it because the two answer different questions: a component says which nodes suit IT, a set says how its children stand to one another, and a field that meant one thing at one level and another at the next would be the sort of thing nobody could read off a manifest.",
	"github.com/ks-tool/horchestra/api/core/v1.SetPlacement.Mode":                                   "Mode is empty (each child placed independently) or PlacementSameNode.",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeMount":                                         "VolumeMount mounts a Volume into the container at MountPath.",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeMount.ReadOnly":                                "ReadOnly mounts a pv volume read-only (a read-only bind under ProtectSystem=strict). tmpfs mounts are always writable ephemeral scratch, so it is ignored for them.",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeMount.SubPath":                                 "SubPath mounts a subdirectory of the volume instead of its root (pv only) — e.g. a per-app subdir of a shared PV. It must be a relative path within the volume (no \"..\", not absolute); the subdirectory is created on demand.",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeSource":                                        "VolumeSource describes a volume inline on an Application, in one shape for every kind (so persistent, ephemeral and secret storage read alike). Type selects the kind: a \"pv\" is backed by a PersistentVolume — created implicitly from Name and Size when none exists, though one may also be created separately and is then used as-is; a \"tmpfs\" is an ephemeral systemd TemporaryFileSystem= with no data on disk, its memory charged to the app's limit; a \"secret\" is populated from a Secret's Data and mounted read-only in RAM.",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeSource.Audience":                               "Audience is who a token volume's token is FOR (token only); empty means this control plane (TokenAudienceAPI). The claim is checked by whoever the token is presented to, so the field is what stops one credential from being every credential — a workload reading the catalog holds a caller at this API, not a login at Vault.",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeSource.DefaultMode":                            "DefaultMode is the file mode for secret keys lacking a per-item Mode (secret only); defaults to 0400.",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeSource.Items":                                  "Items selects and remaps specific keys of a Secret into the mount (secret only); empty projects every key at its own basename.",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeSource.Name":                                   "Name is the referenced object's name: the PersistentVolume (pv) or Secret (secret). For a pv it is optional and defaults to the Application's name; for a secret it is required (admission rejects a nameless secret mount).",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeSource.Optional":                               "Optional, when true, lets the app start even if the referenced Secret is absent (secret only); otherwise a missing secret holds the app pending (fail-closed).",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeSource.Size":                                   "Size is a pv's requested storage or a tmpfs's memory cap (e.g. \"10Gi\", \"64Mi\"). Advisory for a pv in this skeleton; an empty tmpfs size is systemd's default (half of RAM). Rejected for a secret.",
	"github.com/ks-tool/horchestra/api/core/v1.VolumeSource.Type":                                   "Type is \"pv\", \"tmpfs\", \"secret\" or \"token\".",
	"github.com/ks-tool/horchestra/api/core/v1.WeightedWorkloadAffinityTerm":                        "WeightedWorkloadAffinityTerm is a soft workload (anti-)affinity preference.",
	"github.com/ks-tool/horchestra/api/core/v1.WorkloadAffinity":                                    "WorkloadAffinity constrains placement relative to OTHER Applications in the same namespace, within a topology domain. Required must hold (a Filter); Preferred ranks (a Score). One type serves both co-location (workloadAffinity) and spreading (workloadAntiAffinity).",
	"github.com/ks-tool/horchestra/api/core/v1.WorkloadAffinityTerm":                                "WorkloadAffinityTerm selects peer Applications by label (same namespace ONLY — there is deliberately no cross-namespace selector) and a topology domain: TopologyKey is a Node label whose shared value defines the domain (horchestra.io/hostname = per-host).",
	"github.com/ks-tool/horchestra/api/rbac/v1.ClusterRole":                                         "ClusterRole is a cluster-scoped set of PolicyRules, referenced by a ClusterRoleBinding (granting cluster-wide) or by a namespaced RoleBinding (granting the rules within that binding's namespace). It shares RoleSpec with the namespaced Role.",
	"github.com/ks-tool/horchestra/api/rbac/v1.ClusterRoleBinding":                                  "ClusterRoleBinding binds subjects to a ClusterRole cluster-wide — every namespace and cluster-scoped resources. Its RoleRef.Kind is \"ClusterRole\".",
	"github.com/ks-tool/horchestra/api/rbac/v1.PolicyRule":                                          "PolicyRule is one permission: verbs over resources, or verbs over request paths that address no resource. A rule names one or the other, never both — the two are decided by different things (an apiGroup/resource/namespace tuple against a path), and a rule carrying both would read as an intersection while granting the union.",
	"github.com/ks-tool/horchestra/api/rbac/v1.PolicyRule.NonResourceURLs":                          "NonResourceURLs grants request paths that address no object: /metrics, /openapi/v3, discovery. Such a path has no namespace to be scoped to, so only a ClusterRole granted through a ClusterRoleBinding confers them — naming a ClusterRole from a namespaced RoleBinding grants its resource rules in that namespace and none of its paths.\n\nThe verb is the HTTP method, lowercased (\"get\"), because a path has no operation of its own to name. A trailing \"*\" matches by prefix (\"/openapi/*\" covers /openapi/v3 but not /openapi), and a bare \"*\" is every path; nothing else is a pattern.",
	"github.com/ks-tool/horchestra/api/rbac/v1.Role":                                                "Role is a set of permissions inside one namespace. It grants nothing on its own — a RoleBinding is what gives it to someone. For permissions that span namespaces or reach cluster-scoped Kinds, see ClusterRole.",
	"github.com/ks-tool/horchestra/api/rbac/v1.RoleBinding":                                         "RoleBinding grants a Role's permissions to subjects within the binding's own namespace. Its RoleRef may name a Role or a ClusterRole; naming a ClusterRole grants that cluster-wide rule set here only, which is how one definition is reused per namespace without widening it.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStore":                                      "SecretStore is a namespaced reference to an external Vault/OpenBao server. It carries connection and authentication configuration only — no value ever rests in it, so a control-plane compromise leaks addresses and paths, never secrets.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreAuth":                                  "SecretStoreAuth selects and parameterizes the authentication method. It carries no credential: the cert method reuses the node's existing client certificate.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreAuth.Method":                           "Method is the auth method (AuthMethodCert; empty defaults to cert).",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreAuth.Path":                             "Path is the auth engine mount path when it differs from the method name.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreAuth.Role":                             "Role names the server-side role to log in against (e.g. a named cert role). Optional.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreList":                                  "SecretStoreList is a list of SecretStores.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreSpec":                                  "SecretStoreSpec is the \"where and how to reach Vault\": server, KV mount and the auth method a node uses to authenticate as itself.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreSpec.Auth":                             "Auth is how a node authenticates to the server (cert by default).",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreSpec.CABundle":                         "CABundle verifies the server's TLS certificate (PEM). Empty means the system roots.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreSpec.KVVersion":                        "KVVersion selects the KV engine API (1 or 2; default 2).",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreSpec.Mount":                            "Mount is the KV secrets-engine mount path (default \"secret\").",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreSpec.Provider":                         "Provider documents which implementation the server runs (vault or openbao). The two speak the same API, so the built-in client is one implementation; the field records intent. Optional.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreSpec.Server":                           "Server is the base URL, e.g. https://vault.example.com:8200.",
	"github.com/ks-tool/horchestra/api/secrets/v1.SecretStoreSpec.VaultNamespace":                   "VaultNamespace is sent as X-Vault-Namespace (Vault Enterprise namespacing). Optional.",
}
